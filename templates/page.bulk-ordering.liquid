<div class="page-width section-padding">
  <div class="bulk-ordering-container">
    <h1 class="bulk-ordering-title">Bulk Ordering</h1>

    <div class="bulk-ordering-content rte">
      {{ page.content }}
    </div>

    {% if page.content == blank %}
      <div class="bulk-ordering-options">
        <h2>Bulk Ordering Options</h2>
        <ul>
          <li>Minimum order quantity: 10 units</li>
          <li>Tiered pricing for orders of 50 or more units</li>
          <li>Custom packaging and labeling available</li>
          <li>Contact our wholesale team at <a href="mailto:natureburst.shop@gmail.com">natureburst.shop@gmail.com</a> or call us at <a href="tel:+917999839922">+91 79998 39922</a> for a personalized quote.</li>
        </ul>
      </div>
    {% endif %}
  </div>
</div>

<style>
  .bulk-ordering-container {
    background: white;
    padding: 3rem 2rem;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    max-width: 900px;
    margin: 0 auto;
  }
  .bulk-ordering-title {
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--color-primary);
    margin-bottom: 2rem;
    text-align: center;
  }
  .bulk-ordering-content {
    line-height: 1.7;
    margin-bottom: 2rem;
  }
  .bulk-ordering-options h2 {
    font-size: 1.8rem;
    color: var(--color-primary);
    margin-bottom: 1rem;
    text-align: center;
  }
  .bulk-ordering-options ul {
    list-style: disc;
    margin-left: 1.5rem;
  }
  .bulk-ordering-options li {
    margin-bottom: 0.75rem;
  }
  @media (max-width: 767px) {
    .bulk-ordering-container {
      padding: 2rem 1.5rem;
    }
    .bulk-ordering-title {
      font-size: 2rem;
    }
    .bulk-ordering-options h2 {
      font-size: 1.5rem;
    }
  }
</style> 