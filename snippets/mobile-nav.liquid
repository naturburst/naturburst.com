{%- comment -%}
  Enhanced Mobile Navigation Component
  - Fixed close button functionality
  - Improved overlay management
  - Fixed lingering sign-in/register buttons
{%- endcomment -%}

<div id="MobileNav" class="mobile-nav" aria-hidden="true">
  <div class="mobile-nav__inner">
    <div class="mobile-nav__header">
      {% if settings.logo != blank %}
        <img
          class="mobile-nav__logo"
          src="{{ settings.logo | img_url: 'master' }}"
          alt="{{ shop.name | escape }}"
          width="{{ settings.logo_width | default: 140 }}"
          loading="lazy">
      {% else %}
        <span class="h1">Nature<span>Burst</span></span>
      {% endif %}

      <button type="button" class="mobile-nav__close" aria-controls="MobileNav" aria-label="Close menu">
        {% render 'icon-close' %}
      </button>
    </div>

    <div class="mobile-nav__content">
      <!-- Account section -->
      <div class="mobile-nav__section">
        <h3 class="mobile-nav__section-title">
          {% render 'icon-account' %} My Account
        </h3>
        <div class="account-options">
          {% if customer %}
            <a href="{{ routes.account_url }}" class="account-link" data-nav-link>My Account</a>
            <a href="{{ routes.account_logout_url }}" class="account-link" data-nav-link>Log Out</a>
          {% else %}
            <a href="{{ routes.account_login_url }}" class="account-link" data-nav-link>Sign In</a>
            <a href="{{ routes.account_register_url }}" class="account-link" data-nav-link>Register</a>
          {% endif %}
        </div>
      </div>

      <div class="mobile-nav__divider"></div>

      <!-- Currency selector -->
      <div class="mobile-nav__section">
        <h3 class="mobile-nav__section-title">
          {% render 'icon-globe' %} Select Currency
        </h3>
        <div class="currency-options">
          {% form 'currency' %}
            {% for currency in shop.enabled_currencies %}
              <button
                type="submit"
                name="currency_code"
                value="{{ currency.iso_code }}"
                class="currency-option{% if currency.iso_code == cart.currency.iso_code %} active{% endif %}">
                {{ currency.iso_code }} ({{ currency.symbol }})
              </button>
            {% endfor %}
          {% endform %}
        </div>
      </div>

      <div class="mobile-nav__divider"></div>

      <!-- Main navigation -->
      <ul class="mobile-nav__menu">
        {% for link in linklists.main-menu.links %}
          <li class="mobile-nav__item{% if link.active %} mobile-nav__item--active{% endif %}">
            <a href="{{ link.url }}" class="mobile-nav__link" data-nav-link>
              {% case link.title %}
                {% when 'Home' %}
                  {% render 'icon-home' %}
                {% when 'Shop' or 'Products' %}
                  {% render 'icon-shop' %}
                {% when 'How to Use' %}
                  {% render 'icon-help' %}
                {% when 'Contact' %}
                  {% render 'icon-phone' %}
                {% else %}
                  {% render 'icon-link' %}
              {% endcase %}
              <span>{{ link.title }}</span>
            </a>
          </li>
        {% endfor %}
      </ul>

      <div class="mobile-nav__divider"></div>

      <!-- Contact information -->
      <div class="mobile-nav__contact">
        <h3>Contact Us</h3>
        <p>Need help? We're just a click away!</p>
        <a href="{{ routes.page_url | append: 'contact' }}" class="contact-btn" data-nav-link>
          Get in Touch
        </a>
      </div>
    </div>
  </div>
</div>

<!-- Dedicated overlay for mobile menu -->
<div id="MobileNavOverlay" class="mobile-nav-overlay"></div>

<style>
/* Base mobile nav styling */
.mobile-nav {
  position: fixed;
  top: 0;
  left: 0;
  width: 85%;
  max-width: 350px;
  height: 100vh;
  background-color: #ffffff;
  z-index: 9999; /* Higher than overlay */
  transform: translateX(-100%);
  transition: transform 0.3s ease, visibility 0.3s ease; /* Added visibility transition */
  box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
  overflow-y: auto;
  visibility: hidden;
  -webkit-overflow-scrolling: touch; /* Smooth scroll on iOS */
}

.mobile-nav.is-active {
  transform: translateX(0);
  visibility: visible;
}

/* Overlay element */
.mobile-nav-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5);
  z-index: 9998; /* Lower than nav but higher than content */
  opacity: 0;
  visibility: hidden;
  transition: opacity 0.3s ease, visibility 0.3s ease; /* Added visibility transition */
  pointer-events: none;
}

.mobile-nav-overlay.is-active {
  opacity: 1;
  visibility: visible;
  pointer-events: auto; /* Enable interaction when active */
}

/* Prevent body scrolling when menu is open */
body.mobile-menu-open {
  overflow: hidden;
  position: fixed;
  width: 100%;
  height: 100%;
}

/* Inner container structure */
.mobile-nav__inner {
  display: flex;
  flex-direction: column;
  height: 100%;
}

/* Header with logo and close button */
.mobile-nav__header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1.5rem;
  border-bottom: 1px solid rgba(0, 0, 0, 0.1);
  background-color: #ffffff;
}

.mobile-nav__logo {
  height: 60px;
  width: auto;
}

.mobile-nav__header span {
  font-size: 1.5rem;
  font-weight: 800;
  font-family: var(--font-heading);
  color: #1a2e37;
}

.mobile-nav__header span span {
  color: #2A5E41;
}

/* Fixed close button styling */
.mobile-nav__close {
  background: transparent;
  border: none;
  color: #1a2e37;
  font-size: 1.5rem;
  cursor: pointer;
  transition: all 0.2s ease;
  padding: 0.5rem;
  z-index: 10001; /* Make sure it's above other elements */
  position: relative; /* Needed for z-index to work */
  display: flex;
  align-items: center;
  justify-content: center;
}

.mobile-nav__close:hover {
  color: #2A5E41;
  transform: scale(1.1);
}

/* Main content area */
.mobile-nav__content {
  padding: 1.5rem;
  display: flex;
  flex-direction: column;
  gap: 2rem;
  flex: 1;
  background-color: #ffffff;
}

/* Section titles */
.mobile-nav__section-title {
  display: flex;
  align-items: center;
  font-size: 1.1rem;
  margin-bottom: 1rem;
  color: #1a2e37;
  font-weight: 600;
}

.mobile-nav__section-title svg {
  margin-right: 0.75rem;
  color: #2A5E41;
}

/* Account links */
.account-options {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
}

.account-link {
  flex: 1;
  background-color: #f7f7f7;
  border: 1px solid #e5e5e5;
  border-radius: 4px;
  padding: 0.75rem;
  font-size: 1rem;
  font-weight: 500;
  color: #1a2e37;
  text-align: center;
  transition: all 0.3s ease;
  text-decoration: none;
  cursor: pointer;
  position: relative; /* For z-index */
  z-index: 10000; /* High z-index */
}

.account-link:hover {
  background-color: #2A5E41;
  color: #ffffff;
  border-color: #2A5E41;
}

/* Currency selector */
.currency-options {
  display: flex;
  gap: 0.5rem;
}

.currency-option {
  flex: 1;
  background-color: #f7f7f7;
  border: 1px solid #e5e5e5;
  border-radius: 4px;
  padding: 0.75rem;
  font-size: 1rem;
  font-weight: 500;
  color: #1a2e37;
  cursor: pointer;
  transition: all 0.3s ease;
  position: relative; /* For z-index */
  z-index: 10000; /* High z-index */
}

.currency-option.active {
  background-color: #2A5E41;
  color: #ffffff;
  border-color: #2A5E41;
}

.currency-option:not(.active):hover {
  background-color: rgba(42, 157, 143, 0.1);
  color: #2A5E41;
}

/* Divider styling */
.mobile-nav__divider {
  height: 1px;
  background-color: #e5e5e5;
  margin: 0.5rem 0;
}

/* Navigation menu */
.mobile-nav__menu {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.mobile-nav__link {
  display: flex;
  align-items: center;
  padding: 0.75rem 1rem;
  border-radius: 4px;
  transition: all 0.3s ease;
  color: #1a2e37;
  font-weight: 500;
  text-decoration: none;
  cursor: pointer;
  position: relative;
  z-index: 10000; /* High z-index */
}

.mobile-nav__link svg {
  margin-right: 1rem;
  font-size: 1.2rem;
  color: #1a2e37;
  transition: all 0.3s ease;
}

.mobile-nav__link:hover,
.mobile-nav__item--active .mobile-nav__link {
  background-color: rgba(42, 157, 143, 0.1);
  color: #2A5E41;
}

.mobile-nav__link:hover svg,
.mobile-nav__item--active .mobile-nav__link svg {
  color: #2A5E41;
}

.mobile-nav__item--active .mobile-nav__link {
  font-weight: 600;
}

/* Contact section */
.mobile-nav__contact {
  margin-top: auto;
  padding: 1.5rem;
  background-color: rgba(42, 157, 143, 0.1);
  border-radius: 4px;
  text-align: center;
}

.mobile-nav__contact h3 {
  font-size: 1.1rem;
  margin-bottom: 0.5rem;
  color: #1a2e37;
}

.mobile-nav__contact p {
  color: #1a2e37;
  margin-bottom: 1rem;
}

.contact-btn {
  display: inline-block;
  background-color: #2A5E41;
  color: white;
  padding: 0.6rem 1.5rem;
  border-radius: 25px;
  font-weight: 600;
  transition: all 0.3s ease;
  text-decoration: none;
  cursor: pointer;
  position: relative;
  z-index: 10000; /* High z-index */
}

.contact-btn:hover {
  background-color: #234C36;
  transform: translateY(-2px);
}

/* Ensure all interactive elements are properly clickable */
.mobile-nav__link,
.account-link,
.currency-option,
.mobile-nav__close,
.contact-btn {
  -webkit-tap-highlight-color: rgba(0,0,0,0);
  transform: translateZ(0);
  will-change: transform;
}

/* Force hardware acceleration for smooth transitions */
.mobile-nav,
.mobile-nav-overlay {
  transform: translateZ(0);
  backface-visibility: hidden;
  perspective: 1000px;
  -webkit-font-smoothing: antialiased;
}
</style>