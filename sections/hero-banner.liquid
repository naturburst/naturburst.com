<div class="hero-banner">
  <div class="hero-container">
    <div class="hero-content">
      {% if section.settings.show_buttons %}
        <div class="hero-buttons">
          <a href="{{ routes.collections_url }}" class="button button--primary">
            {% render 'icon-cart' %}
            {{ section.settings.shop_button_text | escape }}
          </a>

          {% if section.settings.secondary_button_text != blank and section.settings.secondary_button_url != blank %}
            <a href="{{ section.settings.secondary_button_url }}" class="button button--secondary">
              {{ section.settings.secondary_button_text | escape }}
            </a>
          {% else %}
            <a href="/pages/how-to-use" class="button button--secondary">
              {{ section.settings.secondary_button_text | default: 'HOW TO USE' | escape }}
            </a>
          {% endif %}
        </div>
      {% endif %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Hero Banner",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Desktop Image"
    },
    {
      "type": "image_picker",
      "id": "mobile_image",
      "label": "Mobile Image"
    },
    {
      "type": "checkbox",
      "id": "show_buttons",
      "label": "Show buttons",
      "default": true
    },
    {
      "type": "text",
      "id": "shop_button_text",
      "label": "Shop Button Text",
      "default": "SHOP NOW"
    },
    {
      "type": "text",
      "id": "secondary_button_text",
      "label": "Secondary Button Text",
      "default": "HOW TO USE"
    },
    {
      "type": "url",
      "id": "secondary_button_url",
      "label": "Secondary Button URL",
      "default": "/pages/how-to-use"
    }
  ],
  "presets": [
    {
      "name": "Hero Banner",
      "category": "Image"
    }
  ]
}
{% endschema %}

<style>
.hero-banner {
  position: relative;
  width: 100%;
  overflow: hidden;
  padding: 0;
  margin-top: 0; /* Remove gap between header and hero */
  margin-bottom: 2rem;
  font-family: 'Montserrat', sans-serif;
}

.hero-container {
  background-size: cover;
  background-repeat: no-repeat;
  background-position: center;
  min-height: 600px;
  display: flex;
  align-items: flex-end;
  justify-content: center;
  padding: 2rem 1rem;
  border-bottom: 3px solid #f7f7f7;
}

.hero-content {
  max-width: 1200px;
  width: 100%;
  margin: 0 auto;
  position: relative;
  z-index: 2;
  padding-bottom: 2rem;
}

.hero-buttons {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
  justify-content: center;
}

/* Button styles */
.button {
  display: inline-block;
  padding: 12px 24px;
  background-color: #3f704d; /* Darker green */
  color: white;
  text-transform: uppercase;
  font-weight: bold;
  border-radius: 30px;
  transition: all 0.3s ease;
  border: none;
  text-decoration: none;
  font-family: 'Montserrat', sans-serif;
  font-size: 1rem;
  letter-spacing: 0.5px;
  box-shadow: 0 4px 10px rgba(0,0,0,0.2);
}

.button:hover {
  background-color: #2b4f35;
  transform: translateY(-2px);
  box-shadow: 0 6px 15px rgba(0,0,0,0.25);
}

.button--primary {
  background-color: #3f704d;
  display: flex;
  align-items: center;
  gap: 0.7rem;
}

.button--secondary {
  background-color: white;
  color: #3f704d;
  border: 2px solid #3f704d;
}

.button--secondary:hover {
  background-color: #3f704d;
  color: white;
}

.button .icon {
  font-size: 1.1rem;
}

/* Mobile responsive adjustments - IMPROVED */
@media (max-width: 767px) {
  .hero-container {
    min-height: 450px; /* Fixed height instead of aspect ratio */
    padding-bottom: 120px; /* Extra space for buttons at bottom */
    background-position: center top;
    align-items: flex-end;
  }

  .hero-content {
    padding-bottom: 0;
    display: flex;
    justify-content: center;
    width: 100%;
    position: absolute;
    bottom: 20px; /* Position from bottom */
  }

  .hero-buttons {
    flex-direction: column;
    align-items: center;
    width: 100%;
    max-width: 280px;
    gap: 1rem;
  }

  .button {
    width: 100%;
    justify-content: center;
    padding: 0.75rem 1rem;
  }
}

/* Small mobile devices - IMPROVED */
@media (max-width: 480px) {
  .hero-container {
    min-height: 350px; /* Shorter for smallest screens */
    padding-bottom: 100px; /* Less padding */
  }

  .hero-content {
    bottom: 15px;
  }
}
</style>

<script>
// Optimized image preloading for mobile devices
document.addEventListener('DOMContentLoaded', function() {
  // Only preload when on mobile to save bandwidth
  if (window.innerWidth <= 767) {
    const mobileImageUrl = '{{ section.settings.mobile_image | img_url: 'master' }}';
    if (mobileImageUrl) {
      const img = new Image();
      img.src = mobileImageUrl;
    }
  }
});
</script>